#!/bin/bash

# MCP SOURCES
# - https://hub.docker.com/u/mcp
# - official mcp servers / awesome MCP

# k9s, vim, oh-my-zsh/zsh, git, tree, jq/yq, ruff, sqruff, black/isort, kubectl
# uv, pip, virtualenv, sdkman, ollama, kcctl, dagster (dg), sqlglot / calcite for local query tools + mcp
